# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL (DOCKER)
POSTGRES_USER=sigslab
POSTGRES_PASSWORD=tu_clave_segura_aqui
POSTGRES_DB=sigslab

# CONFIGURACIÓN DE ACCESO DE NESTJS (BACKEND)
DB_HOST=db
DB_PORT=5432
DB_USER=sigslab
DB_PASSWORD=tu_clave_segura_aqui
DB_NAME=sigslab

# CONFIGURACIÓN OPCIONAL PARA BASE DE DATOS EN LA NUBE (ej. Supabase)
# Si usas DATABASE_URL, se ignorarán los campos DB_HOST, DB_PORT, DB_USER, DB_PASSWORD y DB_NAME de arriba.
# DATABASE_URL=postgresql://postgres:[password]@db.[project-id].supabase.co:5432/postgres
# DB_SSL=true (requerido para Supabase y bases de datos seguras en la nube)
DB_SSL=false
